import groovy.transform.CompileStatic

@CompileStatic
class ${className} extends Script {

  ${className}(Binding binding) {
    super(binding)
  }

  static void main(String[] args) {
    new ${className}(new Binding(args)).run()
  }
  Object run() {
    ${scriptText}
  }
}
